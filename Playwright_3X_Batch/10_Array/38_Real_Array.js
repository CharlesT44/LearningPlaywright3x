let browser = ["chrome", "firefox", "safari", "edge", "opera"];
console.log (browser.length);

browser.pop();
// console.log (browser);

// browser.shift();
console.log (browser);

for (i=0; i<browser.length; i++) {
    console.log (browser[i]);
        if (browser[i]==="Opera");{
        console.log("Opera does not spport Automation now");
}
}